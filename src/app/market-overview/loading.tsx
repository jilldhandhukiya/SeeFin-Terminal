import TerminalSkeleton from '@/components/ui/TerminalSkeleton';

export default function MarketOverviewLoading() {
  return <TerminalSkeleton variant="dashboard" title="MARKET_OVERVIEW // STREAMING" />;
}
