import TerminalSkeleton from '@/components/ui/TerminalSkeleton';

export default function TestingTradeLoading() {
  return <TerminalSkeleton variant="split" title="EQUITY_TRADE_DESK // CONNECTING" />;
}
