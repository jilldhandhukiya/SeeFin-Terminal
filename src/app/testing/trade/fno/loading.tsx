import TerminalSkeleton from '@/components/ui/TerminalSkeleton';

export default function TestingTradeFNOLoading() {
  return <TerminalSkeleton variant="split" title="FNO_DERIVATIVES_DESK // INITIALIZING" />;
}
