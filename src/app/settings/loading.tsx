import TerminalSkeleton from '@/components/ui/TerminalSkeleton';

export default function SettingsLoading() {
  return <TerminalSkeleton variant="split" title="SYSTEM_CONFIG // LOADING_PREFERENCES" />;
}
